static int (정적변수) : 함수가 종료되어도 자동으로 소멸하지 않음
지역변수 : 함수가 종료되면 자동으로 소멸된다.
레지스터 : cpu안에 들어있는 아주 빠른 메모리

register int (레지스터 변수) : 레지스터에 저장됨. 
volatile(하드웨어 변수) : 문장을 실행할 때마다 변수의 새로운 값을 메모리에서 읽음 

추가 내용은 수업에서 추가할 것
